;=================================================
; シーンタイトル
;--------------------------------------------
; RESULTS = の後にシーンのタイトルを記述する
;=================================================
@SCENE_TITLE_2
RESULTS = オナニー

;=================================================
; シーン可視条件
;--------------------------------------------
; 特定のキャラクターの存在や、他のシーン結果などを、
; このシーンの表示条件に使用したい時に編集する
;=================================================
@SCENE_VISIBLE_2
; 不可視なら0、可視なら1を戻す
RETURN 1

;=================================================
; シーン報酬
;--------------------------------------------
; シーンの報酬を記述する
; P:1 消費行動ポイント
; P:2 基本消費体力
; P:3 基本消費体力
;=================================================
@SCENE_FEE_2
P:1 = 2
P:2 += 80
P:3 += 50

;=================================================
; シーン解説文
;--------------------------------------------
; シーンの解説文を記述する
;=================================================
@SCENE_INFO_2
DRAWLINE
PRINTL 《オナニー》
PRINTL カメラの前でオナニーを披露してもらいます
DRAWLINE

;=================================================
; シーン成功判定
;--------------------------------------------
; シーンの成功判定を行います
;=================================================
@SCENE_CALC_2

LOCAL:1 = 0

LOCAL:1 += ABL:Ｃ感覚
LOCAL:1 += ABL:信頼
LOCAL:1 += ABL:欲望
LOCAL:1 += ABL:露出癖
LOCAL:1 += ABL:自慰中毒
LOCAL:1 += ABL:被写技能


SIF TALENT:絶倫
	LOCAL:1 += 20
SIF TALENT:露出狂
	LOCAL:1 += 10
SIF TALENT:自慰狂い
	LOCAL:1 += 10
SIF TALENT:マゾ
	LOCAL:1 += 10
SIF TALENT:淫乱
	LOCAL:1 += 5
SIF IS_恥薄い(TARGET)
	LOCAL:1 += 5
SIF TALENT:自慰しやすい
	LOCAL:1 += 5
SIF IS_プライド低い(TARGET)
	LOCAL:1 += 5


SIF TALENT:抵抗
	LOCAL:1 -= 20
SIF TALENT:アイドル
	LOCAL:1 -= 10
SIF IS_恥じらい(TARGET)
	LOCAL:1 -= 5
SIF IS_プライド高い(TARGET)
	LOCAL:1 -= 5


IF LOCAL:1 >= 10
	IF LOCAL:1 >= 50
		RETURN 2
	ELSE
		RETURN 1
	ENDIF
ELSE
	RETURN 0
ENDIF


;=================================================
; シーン撮影開始
;--------------------------------------------
; シーン撮影開始時の地の文を表示します
; 処理の都合上、ここで以下の項目を加算
; P:4 獲得メダル
; P:10 基本獲得ポイント
;=================================================
@SCENE_RESULT_START_2

P:4 = 1
P:10 = 1000
PRINTFORML %NICKNAME:TARGET%こと%NAME:TARGET%のオナニーを撮影した……
PRINTW

;=================================================
; シーン結果・大成功
;--------------------------------------------
; シーン成功判定で2を返した場合の処理を行います
;=================================================
@SCENE_RESULT_PERFECT_2

PRINTFORM %NICKNAME:TARGET%は衆目をはばかることなく、鼻にかかったような甘ったるい
PRINTFORML 嬌声を上げ全身を痙攣させながら、愛液でぐちょぐちょに濡れた性器をいじり続けている……
PRINTL

TIMES (P:10) , 1.25
P:4 *= 2

;=================================================
; シーン結果・成功
;--------------------------------------------
; シーン成功判定で1を返した場合の処理を行います
;=================================================
@SCENE_RESULT_SUCCESS_2

PRINTFORM %NICKNAME:TARGET%はカメラを気にする素振りを見ているが、
SIF IS_恥じらい(TARGET)
	PRINTFORM 頬を赤らて
PRINTFORML 快感に身を震わせながら性器をいじり続けている……
PRINTL

;=================================================
; シーン結果・失敗
;--------------------------------------------
; シーン成功判定で0を返した場合の処理を行います
;=================================================
@SCENE_RESULT_FAILED_2

PRINTFORM %NICKNAME:TARGET%は
IF IS_恥じらい(TARGET)
	PRINTFORM 頬を真っ赤にして
ENDIF
PRINTFORML ぎこちない手つきで性器をいじっていたが、やがてその動きは完全に止まってしまった……
PRINTL

TIMES (P:10), 0.8
P:4 = 0


;=================================================
; シーン経験増加処理
;--------------------------------------------
; シーンの経験増加処理を行います
;=================================================
@SCENE_EXP_2

VARSET LOCAL, 0

LOCAL = (1 + RAND:5)
LOCAL += ABL:自慰中毒
SIF TALENT:自慰狂い
	TIMES LOCAL , 1.25

TCVAR:自慰経験 += LOCAL
TCVAR:調教自慰経験 += LOCAL


IF P:4 != 0 && ABL:Ｃ感覚 != 0
	LOCAL:1 += ABL:Ｃ感覚
	SIF TALENT:自慰狂い
		TIMES LOCAL , 1.50
	SIF ABL:Ｃ感覚 >= LOCAL
		LOCAL:1 = LOCAL
	TCVAR:絶頂経験 += LOCAL:1
ENDIF