;=================================================
; シーンタイトル
;--------------------------------------------
; RESULTS = の後にシーンのタイトルを記述する
;=================================================
@SCENE_TITLE_3
RESULTS = フェラ

;=================================================
; シーン可視条件
;--------------------------------------------
; 特定のキャラクターの存在や、他のシーン結果などを、
; このシーンの表示条件に使用したい時に編集する
;=================================================
@SCENE_VISIBLE_3
;男優処理がないので暫定
IF !HAVE_PENIS(PLAYER) && ITEM:ペニスバンド == 0 
	RETURN 0
ELSE
	RETURN 1
ENDIF

;=================================================
; シーン報酬
;--------------------------------------------
; シーンの報酬を記述する
; P:1 消費行動ポイント
; P:2 基本消費体力
; P:3 基本消費体力
;=================================================
@SCENE_FEE_3
P:1 = 2
P:2 += 80
P:3 += 50

;=================================================
; シーン解説文
;--------------------------------------------
; シーンの解説文を記述する
;=================================================
@SCENE_INFO_3
DRAWLINE
PRINTL 《フェラ》
PRINTL カメラの前でフェラさせます
DRAWLINE

;=================================================
; シーン成功判定
;--------------------------------------------
; シーンの成功判定を行います
;=================================================
@SCENE_CALC_3

LOCAL:1 = 0

LOCAL:1 += ABL:信頼
LOCAL:1 += ABL:欲望
LOCAL:1 += ABL:技巧
LOCAL:1 += ABL:奉仕技術
LOCAL:1 += ABL:奉仕精神
LOCAL:1 += ABL:露出癖
LOCAL:1 += ABL:被写技能

SIF TALENT:精愛味覚
	LOCAL:1 += 20
SIF TALENT:舌使い
	LOCAL:1 += 15
SIF IS_汚臭敏感(TARGET)
	LOCAL:1 += 5
SIF TALENT:献身的
	LOCAL:1 += 5
SIF TALENT:汚れ無視
	LOCAL:1 += 10
SIF TALENT:淫乱
	LOCAL:1 += 5
SIF TALENT:マゾ
	LOCAL:1 += 10
SIF TALENT:露出狂
	LOCAL:1 += 10
SIF TALENT:絶倫
	LOCAL:1 += 20

SIF IS_プライド高い(TARGET)
	LOCAL:1 -= 5
SIF TALENT:抵抗
	LOCAL:1 -= 20
SIF IS_恥じらい(TARGET)
	LOCAL:1 -= 5
SIF IS_汚臭鈍感(TARGET)
	LOCAL:1 -= 10


IF LOCAL:1 >= 10
	IF LOCAL:1 >= 50
		RETURN 2
	ELSE
		RETURN 1
	ENDIF
ELSE
	RETURN 0
ENDIF


;=================================================
; シーン撮影開始
;--------------------------------------------
; シーン撮影開始時の地の文を表示します
; 処理の都合上、ここで以下の項目を加算
; P:4 獲得メダル
; P:10 基本獲得ポイント
;=================================================
@SCENE_RESULT_START_3
P:4 = 1
P:10 = 1000
PRINTFORML %NICKNAME:TARGET%こと%NAME:TARGET%のフェラを撮影した……
PRINTW

;=================================================
; シーン結果・大成功
;--------------------------------------------
; シーン成功判定で2を返した場合の処理を行います
;=================================================
@SCENE_RESULT_PERFECT_3

;男優処理がないので暫定

PRINTFORML %NICKNAME:TARGET%は恍惚とした表情で%CALLNAME:PLAYER%の肉棒をしゃぶり続けている……
PRINTL

TIMES (P:10) , 1.25
P:4 *= 2

;=================================================
; シーン結果・成功
;--------------------------------------------
; シーン成功判定で1を返した場合の処理を行います
;=================================================
@SCENE_RESULT_SUCCESS_3

PRINTFORML %NICKNAME:TARGET%は%CALLNAME:PLAYER%の肉棒に奉仕している……
PRINTL

;=================================================
; シーン結果・失敗
;--------------------------------------------
; シーン成功判定で0を返した場合の処理を行います
;=================================================
@SCENE_RESULT_FAILED_3

PRINTFORML %NICKNAME:TARGET%はぎこちない手つきで奉仕していたが、やがてその動きは完全に止まってしまった……
PRINTL

TIMES (P:10), 0.8
P:4 = 0


;=================================================
; シーン経験増加処理
;--------------------------------------------
; シーンの経験増加処理を行います
;=================================================
@SCENE_EXP_3

VARSET LOCAL, 0

LOCAL = (1 + RAND:5)
LOCAL += ABL:奉仕技術
SIF TALENT:淫乱
	TIMES LOCAL , 1.25

TCVAR:自慰経験 += LOCAL
TCVAR:調教自慰経験 += LOCAL


IF P:4 != 0 && ABL:奉仕精神 != 0
	LOCAL:1 += ABL:奉仕精神
	SIF TALENT:淫乱
		TIMES LOCAL , 1.50
	SIF ABL:Ｃ感覚 >= LOCAL
		LOCAL:1 = LOCAL
	TCVAR:絶頂経験 += LOCAL:1
ENDIF