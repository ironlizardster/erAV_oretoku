;===========================================================
;実績・ロリコン
;===========================================================

; 実績のタイトル
@ACHIEVEMENT_TITLE_1
RESULTS = ロリきゅーぶ

; 実績の達成条件
@ACHIEVEMENT_CALC_1
LOCAL = 0
FOR COUNT, 0, CHARANUM
	SIF COUNT == MASTER
		CONTINUE
	SIF (TALENT:COUNT:童顔 || BASE:COUNT:実年齢 <= 15) && TALENT:COUNT:淫魔 == 0 && TALENT:COUNT:アンドロイド == 0 && TALENT:COUNT:天使 == 0 && CFLAG:COUNT:売却助手可能 == 2
		LOCAL++
NEXT
; 条件を達成している場合は実績番号のグローバルを1にする
IF GLOBAL:1 == 0 && DIFFICULTY_CHECK() && LOCAL >= 4
	GLOBAL:1 = 1
	SAVEGLOBAL
ENDIF
RETURN GLOBAL:1

; 実績の説明および諸々の処理
@ACHIEVEMENT_MAIN_1
; 解説文
PRINTFORML 大勢の高○生未満の女の子たちとキャッキャウフフしたことのある%CALLNAME:MASTER%にぴったりの実績ですね
PRINTFORM とある偉大な
FONTSTYLE 4
PRINTFORM ロリコン
FONTSTYLE 0
PRINTFORML 先人は言いました
PRINTFORML 「まったく、小○生は最高だぜ!!」
PRINTFORMW ……と
; ボーナスなどの処理はここ以降に記述する
IF TALENT:MASTER:ロリコン == 0
	PRINTFORMW そんな%CALLNAME:MASTER%には【ロリコン】の称号を授けましょう
	TALENT:MASTER:ロリコン = 1
ENDIF
