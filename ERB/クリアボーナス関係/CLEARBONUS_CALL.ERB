;===========================================================
;クリアボーナス
;===========================================================
@CLEARBONUS_CALL

CALL ADD_CSV_BIRTHDAY
CALL DEFOLT_GENITALCALL

SIF FLAG:露出癖 == 1
	CALL CLEARBONUS_HENTAI
SIF FLAG:彼氏選択 == 1 && TALENT:TARGET:恋心 == 0
	CALL CLEARBONUS_BOYFRIEND
SIF FLAG:ペニス選択 == 1
	CALL CLEARBONUS_FUTANARI

;===========================================================
;クリアボーナス
;===========================================================
@CLEARBONUS_CALL_MIHIRO
IF NO:TARGET == [[キャラ:新島みひろ]]
	PRINTFORML %CALLNAME:TARGET%が【ONLY ONE IDOL】を取得した状態で開始しますか？
	PRINTL [0] - はい
	PRINTL [1] - いいえ
	$INPUT_LOOP
	INPUT
	
	IF RESULT == 0
		PRINTFORMW %CALLNAME:TARGET%が【ONLY ONE IDOL】を取得した状態で開始します
		TALENT:[[TALENT:ONLY ONE IDOL]] = 1
		MAXBASE:魅力値 += 80
		BASE:魅力値 += 80
		ミッション結果:130 = 4
		MAXBASE:気力 += 1000
		BASE:気力 += 1000
	ELSEIF RESULT == 1
		RETURN
	ELSE
		GOTO INPUT_LOOP
	ENDIF
ENDIF
