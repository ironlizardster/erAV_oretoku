;-------------------------------------------------
;処女を奪うかどうかの確認
;-------------------------------------------------
@CONFIRM_FIRSTKISS
SIF !FLAG:初キス確認
	RETURN 1
IF !CFLAG:初キス相手
	PRINTFORML %CALLNAME:TARGET%の初キスを奪いますか？
	CALL GET_はいいいえ()
	RETURN RESULT == COMMAND_はい
ENDIF
RETURN 1
